# Antigone Architecture

This folder is the home for architecture documentation that should survive implementation churn.

Current key docs still live at repo root for visibility:

- `UNIFIED_REPO_PLAN.md`
- `MEMORY_LAYER.md`
- `INTEGRATION_STATUS.md`

As the repository stabilizes, longer-lived architecture notes can move here without changing the current working systems.
